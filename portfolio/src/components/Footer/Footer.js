import React from "react";
import Button from "../Button/Button";

const Footer = () => {
  return (
    <div>
      The feeter
      <Button text="see the feet" />
    </div>
  );
};

export default Footer;
